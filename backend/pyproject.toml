[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "sector-pulse-backend"
version = "0.1.0"
dependencies = [
    "yfinance",
    "pandas",
    "python-dotenv"
]
